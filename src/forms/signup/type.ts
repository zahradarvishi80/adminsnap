export type FormValues = {
    phoneNumber: string;
    firstName: string;
    lastName: string;
    city: string;
    address: string;
    nationalNumber: string;
    id: number;
    rate: string;
};

export type PropType = {
    submitTxt: string;
};
